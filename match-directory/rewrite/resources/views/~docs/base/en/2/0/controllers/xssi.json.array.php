<p class="hl-danger-block">
    When responding with an array as JSON to a GET request, a JSON hijacking vulnerability may occur, so it's better to send a named array which transforms into an object as JSON.
</p>
